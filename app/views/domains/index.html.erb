<p id="notice"><%= notice %></p>
<%= @list %>

<%= @history %>

<%=form_tag( :controller => :domains, :action => :search ) do |f| %>
    <div class="field">
      <%#= f.label :subject %><br />
      <%= text_field_tag 'name' %>
    </div> 
  <div class="actions">
  <%= submit_tag "check" %>
<% end %>


<h1>Listing Domains</h1>
<!-- <table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <#% @domains.each do |domain| %>
      <tr>
        <td><%#= link_to 'Show', domain %></td>
        <td><%#= link_to 'Edit', edit_domain_path(domain) %></td>
        <td><%#= link_to 'Destroy', domain, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <#% end %>
  </tbody>
</table>
<br>

<%#= link_to 'New Domain', new_domain_path %>
 -->